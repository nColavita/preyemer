import React from "react";

import { StyledCart } from "../../styled/StyledNav";

const Cart = () => {
    return (
        <StyledCart>
            <h4>(0)</h4>
        </StyledCart>
    );
};

export default Cart;
